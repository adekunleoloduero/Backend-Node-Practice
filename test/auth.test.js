const { aclAuth } = require('../src/controllers/authentication');


describe('Authentication functionality', () => {
    test('aclAuth should be defined', () => {
        expect(aclAuth).toBeDefined();
    });

    test('Should have 3 parameters', () => {
        expect(ac
    });
});
